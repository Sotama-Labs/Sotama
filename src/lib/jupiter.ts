// Deprecated: Jupiter Price API was replaced by Pyth Hermes (see oracles.ts)
// since the project moved to devnet-only operation. The canonical mint
// constants moved to tokens.ts (CANONICAL_MINTS / SOL_MINT / USDC_MINT).
// File kept as a sentinel so dangling imports surface as TS errors rather
// than silent runtime failures.

export {};
