node_modules
.next
out
.vercel
.env
.env.local
.env.*.local
.env.mainnet
.env.*.mainnet
*.env.mainnet
.DS_Store
*.tsbuildinfo
.idea
pnpm-lock.yaml
marketing-video
/claude-session.jsonl
/skills-lock.json
brand.md
.claude
# Anchor / Solana
target/
.anchor/
test-ledger/

# Keeper
keeper/target/
keeper/keeper-keypair.json
keeper/.env
keeper/.env.mainnet
keeper/.env.*
!keeper/.env.example

# Generated Turnkey policy — regenerable via `pnpm keeper:policy`.
# Mainnet builds bake the keeper API user UUID into the consensus
# expression; we keep the file local so the UUID isn't pushed to the
# public repo. Source of truth is the Turnkey dashboard.
keeper/turnkey-policy.json

# Program deploy keypair (regenerable, but keep local)
*.so
/.claude/2026-05-05-on-chain-mvp.md
/keeper/Cargo.lock
/Cargo.lock
/.claude/2026-05-06-mainnet-beta-roadmap.md
/keeper/DEPLOY.md
/.claude/2026-05-07-mainnet-final.md
/.claude/2026-05-08-v4-devnet-session.md
/FLY-DEPLOY.md
/MAINNET-UPGRADE-AUTHORITY.md
/MAINNET-LIVE.md

# Program keypairs (regenerable but never commit). Each entry is the
# *identity* of a deployed program, not its upgrade authority. Losing
# one means losing control of that specific program ID.
/keys/
/.claude/fly-lazer-cmd.md

# Internal design docs / brainstorming specs — local only, not in the
# public repo.
/docs/superpowers/
